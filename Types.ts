// Number Type

let price: number;
price = 50;

let amount: number = 50;

// String Type

let firstName: string;
firstName = "Yeasin";

let lastName: string = "Arafath";

let description: string = `First line
Second Line
Third Line`;

let profile: string = `Hello ${firstName} ${lastName}`;

// Boolean Type

let error: boolean = true;
let pending: boolean = false;

// Object Type

let employee: object;

employee = {
  firstName: "Yeasin",
  lastName: "Arafath",
};

let employee1: {
  firstName: string;
  lastName: string;
};

employee1 = {
  firstName: "Yeasin",
  lastName: "Arafath",
};

let employee2: {
  firstName: string;
  lastName: string;
} = {
  firstName: "Yeasin",
  lastName: "Arafath",
};

// Array Tyepe

let skills: string[];
skills = ["firstName", "lastName"];

let skills1 = ["firstName", "lastName"];

let scores: (string | number)[];
scores = ["firstName", 25, "lastName"];

let scores1 = ["firstName", 25, "lastName"];
